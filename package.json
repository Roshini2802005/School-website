{
  "name": "sunrise-school-website",
  "version": "1.0.0",
  "description": "Sunrise International School — Full Stack Website",
  "scripts": {
    "install-deps": "cd backend && npm install",
    "start": "cd backend && node server.js",
    "dev": "cd backend && npm run dev"
  }
}
